# PROJECT-CONTEXT — Akari

雛形: `context/PROJECT-CONTEXT.template.md`。ここには**このプロジェクトだけの事実**を書く。
好みや一般論は `context/` にある。詳細な仕様は `docs/spec/` にある。ここには複製しない。

## 1. これは何か

- 一言で: **LLMにコードを書かせるためのハーネス**。ローカルLLM（OpenAI互換API）を主対象に、デスクトップアプリ・CLI・外部ツール向けAPI（HTTP + MCP）の3つの使い口を持つ。Claude Code / Codex の手元版。
- 誰が使うか: オーナー1人。加えて、オーナーが別に作る**マルチエージェント自動開発ツール**が3つ目の使い口の利用者になる。
- 解決する問題: (1) ローカルLLMサーバの実用的な入口が無い。(2) Claude Code 相当がローカルLLM向けに無い。(3) チャット（Discord等）上で複数エージェントを走らせ成果物をGitでまとめる仕組みを作るとき、各エージェントの「手を動かす部分」を担う土台が要る。
- **作らないもの（非目標）**: モデルの管理／独自推論エンジン／**エージェント同士の会話・司会・役割分担**／**gitのmerge判断・PR作成**／**チャットサービスへの接続そのもの**／マルチユーザー・同期・共有／独自プラグイン機構（MCPに一本化）／モバイル・Web版／音声。詳細は `docs/spec/00-overview.md`。
- 立ち位置: 外部ツールが「会話と司会とmerge」を持ち、Akari が「1エージェント分の手足」を持つ。

## 2. 完成条件

`docs/spec/00-overview.md` の「完成条件」が正本。ここでは複製しない。

## 3. 動かし方

- 対象環境: Windows 10/11、macOS（arm64/x64）、Linux デスクトップ
- 前提: Node.js 20 以上、pnpm 9 以上、ローカルLLMサーバ1つ（Ollama / llama.cpp server / LM Studio / vLLM のいずれか）
- 準備: `pnpm install` → `pnpm build`
- CLI: `node apps/cli/dist/index.js`（`akari` にあたる）
- テスト: `pnpm test`（core 43件 + CLI 14件）
- 型検査: `pnpm typecheck` / 整形: `pnpm format`（対象はコードのみ。文書は整形しない）
- 模擬LLMサーバ: `node tools/mock-llm-server.mjs 11499`（本物が手元に無いとき）
- 動作確認の手順: `docs/getting-started.md`

> デスクトップ（`pnpm dev`）と配布（`pnpm dist`）はまだ無い。P1 以降。

## 4. 構成

- 主要な言語・フレームワーク: TypeScript（strict）、Electron、React、Vite、pnpm workspace
- ディレクトリの役割: `packages/core`（中核: 設定/接続/ツール/エージェント/MCP/記憶/Web/診断）、`apps/desktop`（Electron）、`apps/cli`（CLI）、`apps/server`（ハーネスAPI + MCPサーバ）、`tools/`（開発用の模擬サーバ）、`docs/spec`（仕様）
- 触ってはいけない場所: `context/` と `.claude/skills/` は開発ルールであり、この製品の一部ではない。製品の都合で書き換えない。

## 5. データ

- 保存場所: Windows `%APPDATA%\Akari\` / macOS `~/Library/Application Support/Akari/` / Linux `~/.config/akari/`。`AKARI_HOME` で上書き可
- 形式: JSONファイル。DBを使わない。詳細は `docs/spec/07-data.md`
- 失うと困るもの: `conversations/`、`projects/`、`backups/<runId>/`（取り消しの根拠）
- 移行・バックアップ: `schemaVersion` を持ち、読んだときに1件ずつ移行する。破壊的移行の前にコピーを取る

## 6. 制約

- 性能の目安: 待機時メモリ 300MB 以下（未実測、P9で確認）／会話1000件で一覧が体感で速いこと
- 外部の前提: ローカルLLMサーバはローカルで別途起動されている。Akari は起動・管理をしない
- 鍵: `credentials.json` に平文（ファイル権限 0600）。暗号化ではない。外部APIには `env:` 参照を推奨
- ネットワーク: 待機中は外部へ通信しない。テレメトリ・自動更新なし。外へ出るのは (1) 接続先 (2) MCPサーバ(http) (3) Web検索・取得 の3つだけで、(3) は既定で無効
- ハーネスAPI: `127.0.0.1` 限定 + Bearerトークン + CORS既定全拒否。ファイル編集とコマンド実行を公開する口なので、ここは緩めない
- MCPサーバの登録は「そのプログラムを自分のPCで動かす」ことと同義。Akari のパス境界は効かない
- ライセンス: 依存は MIT / Apache-2.0 / BSD / ISC に限る

## 7. 見た目の方向性

- 系統: **現代的な角丸UI**（`ui.style: "modern"`）。参考が Claude デスクトップアプリのため。古い四角いUIは v1 では実装しない
- 使う色と役割: 主要操作=オレンジ系 / 情報=水色系 / 成功=黄緑系。詳細と色コードは `docs/spec/08-ui.md`
- 対象画面幅: 最小 900px。それ未満では右ペインを畳む

## 8. 決めたこと

| 日付 | 決めたこと | 理由 | 戻すとしたらどうするか |
|---|---|---|---|
| 2026-09-05 | Electron + 共有TSコア（Tauriではなく） | CLIとデスクトップで同じ中核を通せることを最優先。挙動の一致を「同じコード」で保証する | Tauri + Node sidecar。`packages/core` はそのまま使える |
| 2026-09-05 | 保存はJSONファイル。DBなし | ネイティブモジュールが Electron/Node の2 ABI × 3OS へ増えるのを避ける。手で読める | 会話5000件超で SQLite へ。`schemaVersion` を上げ一方向移行 |
| 2026-09-05 | エージェントの出力はイベント列 | 画面・ターミナル・`--json` を同じ順序の同じ情報から作る | — |
| 2026-09-05 | Work = プロジェクト管理 + エージェント実行の両方 | オーナーの指定 | — |
| 2026-09-05 | コマンド実行は書き込みより危険度が高い | `run_command` からファイルを書けるため、書き込みの承認を迂回できてしまう | — |
| 2026-09-05 | 鍵は平文保存（OS権限に依存） | Electron の `safeStorage` は CLI から復号できず、中核共有の前提が崩れる | OS キーチェーンを使う小さなネイティブ層を足す。CLI との共有方法を先に決める必要がある |
| 2026-09-05 | v1 では署名・公証しない | 個人用ツール。証明書の費用と手間が見合わない | 必要になったら Windows/macOS の証明書を用意 |
| 2026-09-05 | `stream:true` を無視するサーバの一括JSON応答も受け付ける | 互換性の実情。エラーにするより本文を出すほうが役に立つ。ログには互換の欠落として残す | 該当分岐を消して `incompatible` に戻す |
| 2026-09-05 | 依存は zod と commander の2つだけ | 検証とCLI引数は自作より既存が堅い。色・ID・テストは自前と標準機能で足りる | どちらも境界が薄く差し替え可能 |
| 2026-09-05 | テストは `node --test` + 型ストリップ、ビルド済み dist に対して実行 | 追加の依存が要らず、配布物そのものを検証できる | vitest 等へ移行 |
| 2026-09-05 | Prettier の対象からすべての `.md` を外す | 手で書いた文書が整形され、意図しない差分が全ファイルに出た | `.prettierignore` から該当行を消す |
| 2026-09-05 | 製品の位置づけを「デスクトップアプリ」から「ハーネス」へ | 外部のマルチエージェントツールから使うことが主用途に加わった。中核とAPIが主で、画面は使い口の1つ | — |
| 2026-09-05 | 実装順序をエージェント先行に変更（デスクトップは P7 へ） | 価値の中心が「手を動かす」ことで、外部ツールから使えるのが最短の実用化。画面は中身ができてから被せるほうが手戻りが少ない | 順序を戻すだけ |
| 2026-09-05 | RAG（ベクトル索引）を非目標から外して入れる | 文脈長の小さいローカルLLMでは、関係する部分だけ渡す仕組みが無いとコードを書かせられない | `memory.enabled` を既定 false のまま使わない |
| 2026-09-05 | ベクトルDBを使わず、フラットファイル＋総当たり検索 | ネイティブモジュールを増やさない方針の一貫。個人用の規模なら足りる見込み（未実測） | 20万チャンク超で近似検索へ |
| 2026-09-05 | 拡張の口はMCPに一本化。独自プラグイン機構は作らない | 口が1つなら安全の検討も1箇所で済む | — |
| 2026-09-05 | Web検索・取得を入れる。ただし既定は無効 | ライブラリの現物仕様が要る。一方でSSRFとプロンプトインジェクションの入口でもある | `web.enabled` を false のままにする |
| 2026-09-05 | ハーネスAPIは localhost 限定＋トークン。`--host` を作らない | ファイル編集とコマンド実行を開ける口だから。遠隔で使うなら利用者がトンネルを張る | — |
| 2026-09-05 | 実行ごとに git worktree を切れるようにする | 複数エージェントが同じリポジトリを踏まないため。merge は外部ツールの仕事 | 隔離せず同じフォルダを使う |
| 2026-09-05 | 実行前に `capabilities.tools` の `auto` を必ず解決する | 未判定のままツール非対応モデルへネイティブのツールを渡すと、何も起きずに終わり、利用者からは原因が分からない | 解決を省いて `auto` を native 扱いに戻す |
| 2026-09-05 | ツール対応の判定結果をモデルごとに覚える（`capabilities.byModel`） | 1つの接続先に複数モデルがぶら下がるのが普通で、対応状況はモデルごとに違う。行き来のたびに判定し直すのは無駄 | `byModel` を見ずに `probedModel` だけで判断する |
| 2026-09-05 | 本文に混ざる `<think>` を受信時に切り分ける | Qwen3 系が主対象モデルになった。素通しすると回答にタグが出るうえ、代替方式で思考の中の下書きをツール呼び出しとして拾う | `ThinkSplitter` を通さず content をそのまま流す |
| 2026-09-05 | 文脈が溢れたら、超過分を5分類（決定/確認済み/棄却案/未解決/次の行動）に圧縮する | 文脈長の小さいモデルが主対象。古い分を黙って捨てると、決めたことと棄却したことを忘れて同じ失敗を繰り返す | `context.mode: "off"` で圧縮せず、入りきらなければ止める |
| 2026-09-05 | 圧縮しても生のメッセージは消さない。検索で戻れるようにする | 圧縮が「失われた」と同じになるのを避ける。圧縮結果の各項目に根拠のメッセージIDを持たせ、なぜそう書いてあるかを追えるようにする | — |
| 2026-09-05 | 圧縮はターン境界でのみ行う | `tool_calls` と対応する `tool` メッセージを切り離すと、OpenAI互換APIが400を返す | — |
| 2026-09-05 | 文脈の圧縮を P2 に置き、ハーネスAPIを P3 へ | 4B級では圧縮が無いと作業が完了しない。完了しない中核を外へ公開しても意味がない | 順序を戻すだけ |
| 2026-09-05 | 模擬サーバを `unref` する | 閉じ忘れたサーバがプロセスを止め、テストの失敗が「ハング」に化けて原因追跡に時間を取られた | `unref()` を外す |

## 9. 今の状態

**P0 完了 / P1 の中核まで完了（いずれも実機確認を除く）**

- 動いているもの:
  - `@akari/core`: 設定の読み書きと検証、鍵の解決（`env:` 参照込み）、OpenAI互換クライアント
    （SSE解釈、ツール呼び出し断片の連結、思考出力の切り分け、エラー分類、
    段階別タイムアウト、機能判定）、
    伏字化、NDJSONログ、診断の書き出し
    - `@akari/core` エージェント: 作業フォルダの境界（シンボリックリンク解決込み）、
    ツール8種、承認の3モード、変更記録とバックアップ、実行単位の取り消し、
    ステップ上限・ループ検出・中断、ツール非対応モデル向けの代替方式（prompted）と
    その自動切り替え
  - `@akari/cli`: `config endpoints add/list/rm/use/probe`、`config list/get/set`、
    `models`、`chat`、`doctor`、`run`、`diff`、`undo`、`runs`
  - `tools/mock-llm-server.mjs`: 開発用の模擬OpenAI互換サーバ
- 検証済み: 自動テスト154件（core 131 / CLI 23）。模擬サーバに対する通し動作。
  特にパス境界18件、ツール18件、実行ループと取り消し21件、代替方式16件を重点的に。
- 実機確認用: `bash tools/verify-local-llm.sh <ベースURL> [モデル...]` で複数モデルを
  比較し、貼れる報告が1ファイルに出る
- **未検証（重要）**: Ollama / llama.cpp / LM Studio / vLLM の**実機**では一度も動かしていない。
  ツール呼び出しのストリーム形式の仮定（`docs/spec/02-provider.md`）は未解決のまま。
  対話モードの `Ctrl+C` は自動テストしていない。
- **未着手（P1の残り）**: 会話の保存（`store`）。実行の変更記録は残るが、やり取りの本文は保存されない
- 未実装: ハーネスAPI（P3）、MCP（P3）、記憶（P4）、Web（P5）、デスクトップ（P7以降）
- 次にやること: 実機での疎通確認（Agents-A1-4B）→ **P1の残り（会話の保存）→ P2（文脈の圧縮と recall）**。
  文脈長の小さいモデルでは、圧縮が無いと数ステップで作業が止まるため、
  ハーネスAPI（P3）より先にする
